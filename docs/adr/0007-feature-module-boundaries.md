---
status: accepted
---

# 按产品功能划分工程模块

GSAULife 使用 `app`、`feature-card` 与 `feature-academic` 三个 Gradle 模块。`app` 负责应用身份、主导航、统一主题、设置、关于与更新；两个功能模块分别拥有校园卡和教务的界面、数据访问与测试。校园卡凭据、教务会话及各自的网络配置继续位于所属功能模块中，共享代码只在出现明确的共同职责后再归入独立模块。这一结构保留两个业务领域的边界，同时让应用级体验由一个入口统一管理。
